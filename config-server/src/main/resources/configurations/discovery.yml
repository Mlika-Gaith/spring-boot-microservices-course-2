eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # don't register eureka server
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 8761
